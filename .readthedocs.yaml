version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.11"

# Build from the readthedocs/ directory with Sphinx
sphinx:
  configuration: readthedocs/source/conf.py

# Explicitly set the version of Python and its requirements
python:
  install:
    - requirements: readthedocs/requirements.txt
